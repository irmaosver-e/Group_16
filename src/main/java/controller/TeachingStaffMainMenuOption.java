package controller;

public enum TeachingStaffMainMenuOption {
    LOGOUT(0),
    MANAGE_RECEIVED_QUERIES(1);

    public final int value;
    TeachingStaffMainMenuOption(int v) {this.value = v;}
}
